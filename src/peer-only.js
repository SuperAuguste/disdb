const fs = require("fs");
const peer = require("./peer");

// setTimeout(() => {

	// peer.upload("testfile.zig", Math.random().toString(36).replace("0.", ""), 0, 10, fs.readFileSync("test/inkscape.exe"));

// }, 2500);
