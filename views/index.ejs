<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Upload</title>

	<link rel="stylesheet" href="/app.css">
</head>
<body>
	
	<main>

		<div id="list">

			<h1>Files</h1>
			
			<div id="files">
				<% for (const file of files) { %>
		
					<div><a href="/download/<%- file %>"><%- file.split("_").slice(0, -1).join("") %></a> <span style="float: right;"><a href="#" onclick="location.replace(`/stream_audio/${prompt('Channel name! (without #)')}/<%- encodeURIComponent(file) %>`);">Play</a> (<%- file.split("_").slice(-1) %>)</span></div>
		
				<% } %>
			</div>
	
		</div>
	
		<div id="upload">
	
			<h1>Upload</h1>
	
			<p>Upload files to Discord below! Please make sure that the files don't contain any private info!</p>
	
			<form ref='uploadForm' 
			id='uploadForm' 
			action='/upload' 
			method='post' 
			encType="multipart/form-data">
				<input type="file" name="foo">
				<input type='submit' value='Upload!'>
			</form>
	
		</div>

	</main>

</body>
</html>